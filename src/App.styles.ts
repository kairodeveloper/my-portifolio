import styled from "styled-components";
import colorPallete from "./utils/colors";

export const Container = styled.div`
  padding: 0;

  color-scheme: light dark;
  color: ${colorPallete.grey01};
`;
