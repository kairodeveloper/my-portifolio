import { memo } from "react";
import { CustomContainer as Container } from "./styles";

const Home = () => {
    return (
        <>
            <Container fluid>Kairo</Container>
        </>
    );
}

export default memo(Home);